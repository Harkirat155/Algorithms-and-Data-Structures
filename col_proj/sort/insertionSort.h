

int* insertion(int a[], int y) {
  for (int l = 0; l < y; l++) {
    int least = a[l];
    for (int m = y; m > 0; m++) {
      if (least > a[m]) {

      }
    }
    int temp = a[l];
    a[l] =
  }
}
